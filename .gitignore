# Built output
app/build/
build/
captures/
out/
release/

# Gradle
.gradle/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# Kotlin
.kotlin/
*.kotlin_metadata

# Android Studio / IntelliJ
.idea/
*.iml
*.iws
*.ipr
.navigation/

# Local SDK location, pulls from local Mac filesystem
local.properties

# Android emulator / signing
*.jks
*.keystore
keystore.properties

# Android profiling
proguard/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Claude Code per-session state
# Keep skills in the repo; everything else under .claude/ is per-machine.
.claude/*
!.claude/skills/
CLAUDE.local.md

# Local-only planning / notes / logs / experiments. Anything in workspace/ is
# scratch that shouldn't leave the machine — matches the sibling repo's rule.
workspace/

# Test reports
app/test-results/
test-results/
reports/

# Crash logs
hs_err_pid*

# NDK
.cxx/

# Generated build files within app
app/release/
app/debug/

# Coverage
coverage/
jacoco.exec

# Release signing — keep keystore + passwords out of the repo
keystore.properties
*.jks
*.keystore
